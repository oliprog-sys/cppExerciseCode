#include <iostream>

using namespace std;

int main(){
    /*int power = 1, base, exponent;

    cout << "Enter the base: ";
    cin >> base;

    cout << "Enter the exponent: ";
    cin >> exponent;

    while(exponent > 0){
        power =  power * base;
        --exponent;
    }

    cout << "The answer is: " << power;*/

    int x;
    for(; x <= 10; x++){
        cout << "HELOO";
    }
    
}